<%- include('index') %>
<h2>Produtos</h2>
<table>
  <tr>
    <th>Nome</th>
    <th>Preço</th>
    <th>Categoria</th>
  </tr>
  <% produtos.forEach(produto => { %>
    <tr>
      <td><%= produto.nome %></td>
      <td><%= produto.preco %></td>
      <td><%= produto.categoria %></td>
    </tr>
  <% }) %>
</table>
